/** Full-width geometric band between major page sections (not under navbar). */
export default function SectionDivider() {
  return <div className="pattern-h-strip w-full shrink-0" aria-hidden />;
}
