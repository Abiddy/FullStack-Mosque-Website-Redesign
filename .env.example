# Vercel Postgres (Storage → Postgres / Neon in Vercel dashboard)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Sheikh admin (/sheikh) — use a long random password
SHEIKH_PASSWORD=

# Session cookie encryption — minimum 32 characters
SESSION_SECRET=
